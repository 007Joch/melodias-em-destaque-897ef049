# Cloudflare Pages Redirects Configuration
# SPA Fallback - todas as rotas não encontradas vão para index.html

# API routes - não redirecionar
/api/* 200

# Assets estáticos - não redirecionar
/assets/* 200
/favicon.ico 200
/robots.txt 200
/*.svg 200
/*.png 200
/*.jpg 200
/*.jpeg 200
/*.webp 200
/*.css 200
/*.js 200

# Uploads do Lovable - não redirecionar
/lovable-uploads/* 200

# Todas as outras rotas vão para index.html (SPA)
/* /index.html 200